---
name: "HD Tile"
description: "A self-similar plane tile with 9 maps at scale 1/3, discovered by Mekhontsev using IFStile. OSC holds; the neighbor graph has only 13 boundary pieces, yet the boundary dimension ≈ 1.993 is the highest known among tiles with such a small neighbor graph — exceeding even the Lévy C curve (dim ≈ 1.934, 41 pieces)."
cardDescription: "Self-similar tile with 9 maps, scale 1/3, and boundary dimension ≈ 1.993."
dimension: "2"
transforms: 9
boundaryDimension: "≈ 1.993"
tags: [plane, tiling, self-similar]
aifs: |
  @
  $n=HD Tile
  $dim=2
  r=[0,1,1,0]
  h1=-1*[-1,0]
  h2=-1*[-2,-1]
  h3=-1*[0,1]
  h4=[-1,-1]
  h5=[1,1]
  h6=[-1,1]
  h7=r*[0,-1]
  h8=r*[1,0]
  A=3^-1*(1|h1|h2|h3|h4|h5|h6|h7|h8)*A
---

import IFSCanvas from '../../components/IFSCanvas.astro';

export const aifsHD = `@
\$n=HD Tile
\$dim=2
r=[0,1,1,0]
h1=-1*[-1,0]
h2=-1*[-2,-1]
h3=-1*[0,1]
h4=[-1,-1]
h5=[1,1]
h6=[-1,1]
h7=r*[0,-1]
h8=r*[1,0]
A=3^-1*(1|h1|h2|h3|h4|h5|h6|h7|h8)*A`;

## Overview

The **HD Tile** is a self-similar plane tile generated by 9 affine maps, all with contraction
ratio $\frac{1}{3}$. Its Hausdorff dimension is $2$ and it tiles the plane by translation.

The tile is visually striking because the mix of orientations — including maps with
central symmetry ($-1$) and axis-swapping reflections ($r$) — makes the IFS look as if
its images overlap. In fact, the **Open Set Condition** holds exactly: the neighbor
graph has only 13 pairwise boundary components.

What makes the HD Tile exceptional is the combination of a **tiny neighbor graph** — only
13 pairwise boundary pieces — and a **boundary dimension ≈ 1.993**, the positive root of

$$x^7 - 12x^6 + 27x^5 + 10x^4 - 61x^3 + 20x^2 + 9x - 18 = 0.$$

For comparison, the [Lévy C curve](/ifs/levy-c-curve) — widely regarded as one of the
roughest self-similar boundaries — has dim ≈ 1.934 but requires **41 boundary pieces**
to encode its neighbor structure. The HD Tile exceeds that boundary dimension with fewer
than a third of the pieces. The boundary is an extremely space-filling fractal curve —
it almost fills the plane while still having zero area.

Discovered by D. Mekhontsev using the IFStile search engine.

## Structure of the Nine Maps

The attractor satisfies:

$$A = \tfrac{1}{3}\bigl(1 \cup h_1 \cup h_2 \cup h_3 \cup h_4 \cup h_5 \cup h_6 \cup h_7 \cup h_8\bigr)(A)$$

The nine maps fall into three groups by their linear part:

| Maps | Linear part | Description |
|------|------------|-------------|
| $1$ | identity | no rotation |
| $h_1, h_2, h_3$ | $-1$ (central symmetry) | 180° rotation |
| $h_4, h_5, h_6$ | identity | pure translations |
| $h_7, h_8$ | $r = \begin{pmatrix}0&1\\1&0\end{pmatrix}$ | axis-swap reflection |

The translations are:

$$h_1: [1,0], \quad h_2: [2,1], \quad h_3: [0,-1]$$

$$h_4: [-1,-1], \quad h_5: [1,1], \quad h_6: [-1,1]$$

$$h_7(x) = r\,x + [-1,0], \quad h_8(x) = r\,x + [0,1]$$

## Boundary Dimension

Computing the neighbor graph (331 intersection checks, exact rational arithmetic)
confirms the OSC holds and yields 13 pairwise boundary pieces $i_1, \ldots, i_{13}$.
Piece $i_{11}$ is a point contact (dim = 0); the remaining 12 pieces share dimension
governed by the degree-7 polynomial above, giving

$$\dim_H(\partial A) \approx 1.993 = \frac{2\log(8.935\ldots)}{\log 9}.$$

The boundary is thus nearly 2-dimensional — it almost saturates the ambient plane.
Achieving dim > 1.99 with only 13 neighbor pairs (versus 41 for the Lévy C curve) makes
the HD Tile stand out among known self-similar plane tiles.

## Zoomed Boundary Detail

The following canvas is zoomed to a small region of the boundary, making its near-planar
fractal texture visible. The default canvas at the top of the page shows the full tile.

The zoom center is at approximately $(0.134,\,-0.106)$ — near the lower-right area of
the tile — at a viewport radius of $0.0013$, corresponding to a **≈ 716× magnification**
relative to the full-tile view (bounding radius ≈ 0.930).

<IFSCanvas id="hd-tile-zoom" aifs={aifsHD} width={480} height={480}
  label="HD Tile — zoomed boundary"
  camera={[0.1343992682175182, -0.1055559612504252, 0.001297772296920, 0]} />
